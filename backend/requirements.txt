# Runtime dependencies — pinned per docs/security.md §8 (no silent drift).
# Upgrades are deliberate, reviewed changes.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
prometheus-client==0.25.0
clickhouse-connect==1.5.0
