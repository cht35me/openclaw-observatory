# M004 PR validation artifacts

Headless-Chromium screenshots attached to M004 PR reviews (zero browser
console errors at capture time). Temporary review evidence — may be removed
before merge at the supervisor's discretion.
